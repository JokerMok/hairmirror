# 上线底座验证记录

验证日期：2026-07-15

## 自动化结果

- ESLint：通过；
- TypeScript：通过；
- Vitest：15 个测试文件、34 项测试全部通过；
- Next.js 生产构建：通过，无构建警告；
- 生产依赖审计：0 个已知漏洞；
- Worker、发布和回滚脚本语法检查：通过。

## 独立环境演练

演练使用 `/tmp/hair-launch-e2e` 下的独立 SQLite、原图、结果和备份目录，模型固定为本地演示模型，未调用真实生图接口。

| 项目                   | 结果                                                              |
| ---------------------- | ----------------------------------------------------------------- |
| 健康检查               | 返回 200 和 `ok`                                                  |
| 内部 Worker 未授权访问 | 返回 401                                                          |
| 首次生成请求           | 返回 202 和排队状态                                               |
| 相同幂等键重复请求     | 返回同一任务，不新增队列记录                                      |
| Worker 消费            | 完成一个任务后返回空闲                                            |
| 私有字段泄漏检查       | 响应不含游客会话和用户 ID                                         |
| 方案与反馈             | 选择、反馈写入后可查询                                            |
| 进程重启恢复           | 重启后任务、结果和选择仍可读取                                    |
| 备份                   | 数据库完整性检查通过并生成清单                                    |
| 维护任务               | 僵死恢复、到期任务和计划备份正常执行                              |
| 真删除                 | 删除后接口返回 404，任务、反馈和选择归零，队列载荷脱敏            |
| 数据回滚               | 恢复后任务、反馈和选择各 1 条，`PRAGMA integrity_check` 返回 `ok` |

## 对抗式复核修正

- 修正了监控事件写入失败可能反向影响核心任务事务的问题；
- 修正了告警 Webhook 异常可能拖垮 Worker 的问题；
- 修正了任务提交事务在提交后记录事件失败时错误回滚的问题；
- 补齐了 30 天任务、方案、选择和反馈元数据清理；
- 修正了自定义生成图片目录下真删除路径不一致的问题；
- 将备份文件操作隔离到专用脚本，避免生产构建误追踪整个项目目录。

## 发布边界

该结果支持单机受控内测。公开测试仍需完成真人黄金样本评测、HTTPS 与正式告警通道验证、异地备份以及托管数据库和私有对象存储迁移。
