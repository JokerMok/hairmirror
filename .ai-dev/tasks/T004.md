# T004 Salon 咨询创建与主流程

实现 Stylist 创建咨询、上传照片、调用分析/推荐、展示方案和保存状态的 UI/API 主流程。所有写操作使用幂等键并绑定 salon_id/created_by。

验收：完整 happy path 可刷新恢复；验证失败有可修复提示；未授权拒绝；状态可从 draft 到 completed/partial/failed。

允许路径：`src/app/salon/consultations/new/page.tsx`、`src/components/salon-consultation-flow.tsx`、`src/app/api/consultations`、`src/app/api/consultations/route.ts`（仅本任务新增/修改的咨询 API 文件）、`tests/api/consultations.test.ts`、`tests/ui/salon-consultation-flow.test.tsx`

快照排除目录：`.next/`、`node_modules/`、`coverage/`、`test-results/`。
