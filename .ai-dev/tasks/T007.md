# T007 生成队列接入与部分失败体验

把咨询推荐接入现有生成队列；每个推荐独立状态，支持取消、重试和错误补偿，保证失败不会扣除有效权益；旧设计任务保持回归。

允许路径：`src/components/studio-wizard.tsx`、`src/lib/generation-provider.ts`、`src/lib/generation-queue.ts`、`tests/unit/generation-consultation.test.ts`、`tests/e2e/generation-fallback.spec.ts`

快照排除目录：`.next/`、`node_modules/`、`coverage/`、`test-results/`。
