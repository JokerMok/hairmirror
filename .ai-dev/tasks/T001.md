# T001 基础领域模型、租户边界与 feature flags

建立 Salon、Consultation、Recommendation 的类型、迁移和服务端授权基础；收费暂停开关只提供底层配置，不改页面。

验收：迁移可重复执行；同租户/跨租户授权单测通过；咨询状态与错误码有穷举；旧设计任务和账单读取不回归。

允许路径：`src/lib/types.ts`、`src/lib/database.ts`、`src/lib/consultation-domain.ts`、`src/lib/feature-flags.ts`、`tests/unit/consultation-domain.test.ts`、`tests/unit/feature-flags.test.ts`、`documentation/02-technical/consultation-data-model.md`

快照排除目录：`.next/`、`node_modules/`、`coverage/`、`test-results/`。
