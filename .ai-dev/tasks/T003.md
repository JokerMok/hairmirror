# T003 AI 分析与推荐契约

新增 schema 校验、provider 接口、fixture 和失败 fallback。分析必须输出脸型/头型/发质/当前特点/置信度/限制；推荐最多三项并含维护时长和执行建议。

验收：无效模型输出不能进入 UI；低置信度可解释；不包含身份、医疗或敏感推断；provider 超时返回可展示的 fallback 状态。

允许路径：`src/lib/ai-consultation.ts`、`src/lib/ai-consultation-schema.ts`、`src/lib/ai-consultation-fallback.ts`、`tests/unit/ai-consultation.test.ts`、`tests/unit/ai-consultation-schema.test.ts`

快照排除目录：`.next/`、`node_modules/`、`coverage/`、`test-results/`。
