# T006 沟通卡

从已确认推荐生成结构化沟通卡，支持复制并提供字段确认提示；PNG/PDF 只保留接口占位，不在 P0 强行实现。

允许路径：`src/lib/communication-card.ts`、`src/components/communication-card.tsx`、`src/app/api/consultations`（仅本任务新增/修改的卡片 API 文件）、`tests/unit/communication-card.test.ts`、`tests/ui/communication-card.test.tsx`

快照排除目录：`.next/`、`node_modules/`、`coverage/`、`test-results/`。

状态：已完成（2026-07-30）

验收：已接入咨询沟通卡 API、页面展示和一键复制；PNG/PDF 保持后续接口位。
