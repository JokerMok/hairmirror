# T002 角色入口与 Salon/Consumer 路由

重构首页定位文案和入口，增加 Stylist/Consumer 选择及最小路由壳；保留现有生成体验入口，未登录受限操作导向登录。

验收：桌面和 375px 布局无横向滚动；角色选择可恢复；导航和登录跳转可用；不包含收费 CTA。

允许路径：`src/app/page.tsx`、`src/components/home-showcase.tsx`、`src/components/role-entry.tsx`、`src/app/salon/page.tsx`、`src/app/consumer/page.tsx`、`tests/ui/role-entry.test.tsx`

快照排除目录：`.next/`、`node_modules/`、`coverage/`、`test-results/`。
