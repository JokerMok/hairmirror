# T008 暂停收费入口

用 feature flag 隐藏 Paddle、次数包和订阅 CTA，保留已有 billing API、数据和后台读取；关闭 flag 时旧账单接口继续正常响应。

允许路径：`src/app/pricing/page.tsx`、`src/lib/billing-feature-flags.ts`、`tests/unit/billing-feature-flags.test.ts`

快照排除目录：`.next/`、`node_modules/`、`coverage/`、`test-results/`。
